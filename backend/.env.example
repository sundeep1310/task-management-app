# Server Configuration
PORT=5000
NODE_ENV=development

# Data Storage
DATA_DIR=./data
# Set to 'true' to disable file storage on platforms with ephemeral filesystems like Render
DISABLE_FILE_STORAGE=false

# CORS Configuration
# Add additional allowed origins separated by commas if needed
ALLOWED_ORIGINS=https://your-frontend-domain.com,http://localhost:3000
KEEP_ALIVE=true
SERVER_URL=https://your-render-url.onrender.com